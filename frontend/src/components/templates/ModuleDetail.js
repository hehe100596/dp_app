import React from "react";

export function ModuleDetail({ moduleId }) {
  // TODO: Confirm user really has access to this module!

  return (
    <div align="center">
      ---
      {moduleId}
      ---
    </div>
  );
}
